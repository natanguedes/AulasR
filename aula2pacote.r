install.packages("caret")#para algoritmo machine learning

install.packages("randomForest")#para algoritmo machine learning, O randomForest implementa o algoritmo de floresta aleatória, grupo de algoritmo de aprendizado de máquina
install.packages("ggplot2") #visualização de gráficos
install.packages("dplyr")#pacote de manipulação de dados
install.packages("devtools")#permites instalação de pacote

library(ggplot2) #diz ao r que é pra usar o pacote ggplot2
detach(package:ggplot2) #descarrega o pacote ou seja, o mesmo não pode ser mais utilizado nessa sessão

help(mean)#ajuda da m?dia
#busca mais informa??o sobre a fun??o
install.packages("sos")

#usa a fun??o na sess?o
library(sos)
library(brew)
#como utilizar essa fun??o
findFn("fread")
#faz uma busca no RSite
RSiteSearch("matplotlib")
#mostra um exemplo da fun??o ou pacote
example("mean")
example("matplot")

